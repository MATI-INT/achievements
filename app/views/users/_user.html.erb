<li>
  <%= gravatar_for user %>
  <%= user.name %>
  (<%= user.email %>)
  <%= link_to 'Delete', user_path(user),
              method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure?'} %>
</li>