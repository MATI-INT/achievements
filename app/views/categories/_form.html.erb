<%= form_for @category do |f| %>
  <%= render 'shared/errors', object: @category %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit %>
<% end %>